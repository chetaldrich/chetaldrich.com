= wrap_layout :layout do
  .post
    .post-brand
      %a{:href => "/"} chetaldrich
      %h1= current_article.title || "" unless current_article.nil?
    .postcontent
      %article= yield
      %hr
      %a{:href => "/blog"} &larr; More Posts
